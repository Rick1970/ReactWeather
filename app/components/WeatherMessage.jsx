var React = require('react');

var WeatherMessage = React.createClass({
  render: function() {
      return (
        <p>This is where the weather will display.</p>
      );
  }
});
module.exports = WeatherMessage;
